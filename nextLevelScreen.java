package com.ec327.android.tuneplay;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;

public class nextLevelScreen extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_next_level_screen);
    }

    // This method is called when the Next Level button is pushed
    public void nextLevel(View view) {
        // code to take app to next level
    }
}
